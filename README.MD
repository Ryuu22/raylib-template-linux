Just a tiny boilerplate to bootstrap my tiny raylib games.

## How to use:

1. Create build folder and configure CMake.

```bash
mkdir build
cd ./build
cmake ..
```

2. Build your game

```bash
cmake --build .
```

3. Run with the default name

```bash
./NewGame
```